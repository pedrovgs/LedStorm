name: Check Swift app
on: [pull_request, push]
jobs:
  build:
    runs-on: macOS-10.15
    steps:
    - uses: actions/checkout@v2
    - name: Install carton
      run: brew install swiftwasm/tap/carton
    - name: Build
      run: carton bundle
